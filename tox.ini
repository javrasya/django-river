[tox]
envlist = py27

[testenv:py27]
deps =
    pytest-django
    pytest-cov
    -rrequirements.txt
commands = py.test --cov ./ --cov-report term-missing  --ds='test_settings' --junitxml=../junit-{envname}.xml







